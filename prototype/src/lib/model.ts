/* The one place the model is named. Keep server + any client reference in sync.
   Sonnet 4.6 is what the pedagogy was tuned on; Sonnet 5 is available if we
   ever choose to re-tune and upgrade. Don't swap it casually. */
export const MODEL = "claude-sonnet-4-6";
export const MAX_TOKENS = 4000;
